<header
  class="text-base bg-tan bg-opacity-90 px-6"
>
  <div
    class="flex flex-col items-center py-2 mx-auto md:flex-row "
  >
    <a
      class="flex items-center text-gray-900"
    >
      <img class="w-20" src="logo.png" />
      <span class="text-xl">Megnolia Codes</span>
    </a>

    <nav class="space-x-3 md:ml-auto md:pr-10 md:space-x-10">
      {[
        ['About', '/about'],
        ['Skills', '/skills'],
        ['Work', '/work'],
        ['Contact', '/contact'],
      ].map(([title, url]) => (
        <a href={url} class="hover:text-orchid relative before:content-[''] before:absolute before:block before:w-full before:h-[4px] before:bottom-0 before:left-0 before:bg-peach before:hover:scale-x-100 before:scale-x-0 before:origin-center before:transition before:ease-in-out before:duration-300">{title}</a>
      ))}
    </nav>
  </div>
</header>
